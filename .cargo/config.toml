[build]
# Cross-compile targets configured via rust-toolchain.toml

# ARM cross-compile linkers (used when building with cargo directly, not cross)
[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"

[target.armv7-unknown-linux-gnueabihf]
linker = "arm-linux-gnueabihf-gcc"
